apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-auth-app
  labels:
    app: ecommerce-auth-app
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: ecommerce-auth-app
  template:
    metadata:
      labels:
        app: ecommerce-auth-app
    spec:
      imagePullSecrets:
      - name: ghcr-pull-secret
      containers:
      - name: ecommerce-auth-app
        image: ghcr.io/ilhamzefer-sketch/ecommerce-auth:6778d53bc959ad400ca01385d4c418ec518fffd4
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "postgres"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres-service:5433/ecommerce?currentSchema=ecommerce_auth"
        - name: SPRING_DATASOURCE_USERNAME
          value: "postgres"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "password"
        - name: SPRING_FLYWAY_SCHEMAS
          value: "ecommerce_auth"
        - name: SPRING_FLYWAY_CREATE_SCHEMAS
          value: "true"
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: ecommerce-jwt-secret
              key: secret-key
        - name: REFRESH_COOKIE_SECURE
          value: "false"
        - name: API_GATEWAY_PUBLIC_URL
          value: "http://localhost:8080"
        - name: SPRINGDOC_SWAGGER_UI_SYNTAX_HIGHLIGHT_THEME
          value: "tomorrow-night"
---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce-auth-service
spec:
  type: ClusterIP
  selector:
    app: ecommerce-auth-app
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
