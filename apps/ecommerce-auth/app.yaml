apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-auth-app
  labels:
    app: ecommerce-auth-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecommerce-auth-app
  template:
    metadata:
      labels:
        app: ecommerce-auth-app
    spec:
      imagePullSecrets:
      - name: ghcr-pull-secret
      containers:
      - name: ecommerce-auth-app
        image: ghcr.io/ilhamzefer-sketch/ecommerce-auth:04268b4b539ec3298893d1bf9b81039dad0dd559
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "postgres"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres-service:5433/ecommerce?currentSchema=ecommerce_auth"
        - name: SPRING_DATASOURCE_USERNAME
          value: "postgres"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "password"
        - name: SPRING_FLYWAY_SCHEMAS
          value: "ecommerce_auth"
        - name: SPRING_FLYWAY_CREATE_SCHEMAS
          value: "true"
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: ecommerce-jwt-secret
              key: secret-key
        - name: REFRESH_COOKIE_SECURE
          value: "false"
---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce-auth-service
spec:
  type: ClusterIP
  selector:
    app: ecommerce-auth-app
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
